{
  "dependencies": {
    "axios": "^0.18.0",
    "axios-cookiejar-support": "^0.4.2",
    "cheerio": "^1.0.0-rc.3",
    "consola": "^2.5.8",
    "cookie": "^0.3.1",
    "nexe": "^3.0.1",
    "puppeteer-core": "^1.14.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "request-promise-any": "^1.0.7",
    "set-cookie-parser": "^2.3.5",
    "typescript": "^3.4.1"
  },
  "scripts": {
    "dev": "nodemon --watch src/**/*.* --exec ts-node ./src/index.ts",
    "test": "nodemon --watch **/*.ts --exec ts-node",
    "debug": "nodemon --watch src/**/*.ts --exec node --inspect -r ts-node/register ./src/index.ts",
    "build": "rm -rf ./build && tsc && node build.js"
  },
  "devDependencies": {
    "@types/node": "^11.13.0",
    "nodemon": "^1.18.10",
    "ts-node": "^8.0.3"
  }
}
